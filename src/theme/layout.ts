/** Caps the phone-oriented layout's width so it reads as a centered
 * column instead of stretching edge-to-edge on wide desktop browser
 * windows — the app was designed mobile-first and every screen assumes
 * roughly this width. */
export const MAX_CONTENT_WIDTH = 480;
